# Streamlit Community Cloud — install from repo root.
# Pinned to versions verified locally (Python 3.12, 2026-09-05).
# Only packages imported by app/**/*.py: streamlit, pandas, plotly, numpy.
streamlit==1.43.2
pandas==3.0.5
plotly==6.3.0
numpy==2.5.2
