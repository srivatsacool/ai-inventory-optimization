---
// The central research question — the hero's first statement.
---

<h1 class="headline">
  <span class="headline__line" data-reveal-line>Can more advanced</span>
  <span class="headline__line" data-reveal-line><em class="headline__em">AI</em> actually improve</span>
  <span class="headline__line" data-reveal-line>inventory decisions?</span>
</h1>

<style>
  .headline {
    font-size: clamp(2rem, 4vw, 3.2rem);
    font-weight: 500;
    letter-spacing: var(--track-display);
    line-height: var(--lh-display);
    max-width: 24ch;
    margin: 0;
    color: var(--ink);
  }
  .headline__line {
    display: block;
  }
  .headline__em {
    font-style: italic;
    color: var(--c-llm);
  }
</style>